<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to (I18n.t"boober.page.parts.view my profile"), current_user, data: {"turbolinks" => false} %></span>
<span><%= pluralize(current_user.books.count, "Book") %></span>
